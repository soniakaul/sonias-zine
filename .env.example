# Inky needs this to answer questions (server-side, never exposed to the browser).
# Get a key at https://console.anthropic.com → API Keys.
#
# DO NOT put your real key here — this file is a committed template.
# Put the real key in .env.local (gitignored), which Next.js loads automatically.
ANTHROPIC_API_KEY=sk-ant-...
